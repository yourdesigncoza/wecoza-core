---
phase: 55-agent-attendance-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Agents/Controllers/AgentAccessController.php
  - views/agents/attendance/agent-attendance.view.php
autonomous: true
requirements: [AGT-05, AGT-06]

must_haves:
  truths:
    - "Agent sees only their assigned classes (primary + backup) on the attendance page"
    - "Shortcode renders class list with links to existing single-class attendance UI"
    - "Agent with no agent record sees a clear error message, not a crash"
  artifacts:
    - path: "src/Agents/Controllers/AgentAccessController.php"
      provides: "Shortcode registration, class lookup, page auto-creation"
      exports: ["agentAttendanceShortcode", "ensureAttendancePage", "getClassesForAgent"]
    - path: "views/agents/attendance/agent-attendance.view.php"
      provides: "Agent-facing class list view"
      min_lines: 30
  key_links:
    - from: "src/Agents/Controllers/AgentAccessController.php"
      to: "AgentRepository::findByWpUserId()"
      via: "WP user ID to agent_id resolution"
      pattern: "findByWpUserId"
    - from: "src/Agents/Controllers/AgentAccessController.php"
      to: "wecoza_db()->getAll()"
      via: "JSONB containment query for class_agent + backup_agent_ids"
      pattern: "backup_agent_ids.*@>"
    - from: "views/agents/attendance/agent-attendance.view.php"
      to: "/app/display-single-class/?class_id=X"
      via: "Link per class row to existing single-class page"
      pattern: "display-single-class"
---

<objective>
Create the AgentAccessController with the `[wecoza_agent_attendance]` shortcode that queries classes assigned to the logged-in agent (primary + backup via JSONB containment) and renders them in a minimal class list view. Auto-creates the `/agent-attendance/` WP page if missing.

Purpose: AGT-05 (agent sees only their classes) and AGT-06 (dedicated shortcode with existing attendance UI)
Output: AgentAccessController.php, agent-attendance.view.php, auto-created WP page
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-agent-attendance-page/55-RESEARCH.md

@src/Agents/Repositories/AgentRepository.php (findByWpUserId at line 269)
@src/Agents/Controllers/AgentsController.php (pattern: controller initialization, registerShortcodes)
@src/Classes/Controllers/ClassController.php (pattern: ensureRequiredPages, registerShortcodes)

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/Agents/Repositories/AgentRepository.php line 269:
```php
public function findByWpUserId(int $wpUserId): ?array
// Returns agent row array or null. Key field: $agent['agent_id']
```

From core/Database/PostgresConnection (via wecoza_db()):
```php
public function getAll(string $sql, array $params = []): array|false
```

From ClassController::ensureRequiredPages() pattern:
```php
// Transient-guarded page creation on init hook
// Guard: current_user_can('manage_options') && !get_transient(...)
// set_transient(..., true, HOUR_IN_SECONDS)
// get_page_by_path('slug') check before wp_insert_post()
```

Existing shortcode registration pattern (AgentsController):
```php
add_action('init', [$this, 'registerShortcodes']);
```

Plugin bootstrap (wecoza-core.php ~line 305):
```php
// Initialize Agents Module
if (class_exists(\WeCoza\Agents\Controllers\AgentsController::class)) {
    new \WeCoza\Agents\Controllers\AgentsController();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentAccessController with shortcode and class lookup</name>
  <files>src/Agents/Controllers/AgentAccessController.php</files>
  <action>
Create `AgentAccessController` in `WeCoza\Agents\Controllers` namespace.

Constructor calls:
- `add_action('init', [$this, 'registerShortcodes']);`
- `add_action('init', [$this, 'ensureAttendancePage']);`

Methods:

1. `registerShortcodes()` — registers `[wecoza_agent_attendance]` shortcode mapped to `agentAttendanceShortcode()`

2. `ensureAttendancePage()` — mirrors ClassController::ensureRequiredPages() pattern:
   - Guard: `if (!current_user_can('manage_options') || get_transient('wecoza_agent_attendance_page_checked')) return;`
   - `set_transient('wecoza_agent_attendance_page_checked', true, HOUR_IN_SECONDS);`
   - Check `get_page_by_path('agent-attendance')` — if null, `wp_insert_post()` with title "Agent Attendance", content `[wecoza_agent_attendance]`, slug `agent-attendance`, status `publish`

3. `agentAttendanceShortcode(): string` — the shortcode handler:
   - Check `current_user_can('capture_attendance')` — if not, return alert-danger div "Access denied."
   - Call `resolveAgentId()` — if null, return alert-warning div "Agent profile not found. Contact administrator."
   - Call `getClassesForAgent($agentId)` to get assigned classes
   - Return `wecoza_view('agents/attendance/agent-attendance', ['classes' => $classes, 'agentId' => $agentId], true)`

4. `private resolveAgentId(): ?int` — resolution chain:
   - `$wpUserId = get_current_user_id();` — return null if 0
   - `$repo = new \WeCoza\Agents\Repositories\AgentRepository();`
   - `$agent = $repo->findByWpUserId($wpUserId);`
   - Return `$agent ? (int) $agent['agent_id'] : null`

5. `private getClassesForAgent(int $agentId): array` — JSONB query:
   - Build `$jsonFragment = json_encode([['agent_id' => (int) $agentId]]);` (cast to int per research pitfall 4)
   - SQL query from research Pattern 1: SELECT class_id, class_code, class_type, class_subject, class_status, class_agent, backup_agent_ids, schedule_data, stop_restart_dates, learner_ids, original_start_date FROM classes WHERE (class_agent = :agent_id OR backup_agent_ids::jsonb @> :json_frag) AND class_status != 'deleted' ORDER BY original_start_date DESC
   - Params: `:agent_id` => $agentId, `:json_frag` => $jsonFragment
   - Return `wecoza_db()->getAll($sql, $params) ?: []`

Do NOT extend BaseController — follow AgentsController pattern (standalone class). Use `declare(strict_types=1)` and PHP 8.0+ typed properties/return types.
  </action>
  <verify>
    <automated>php -l src/Agents/Controllers/AgentAccessController.php</automated>
  </verify>
  <done>AgentAccessController.php exists with shortcode registration, page auto-creation, agent resolution, and JSONB class lookup. No syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create agent attendance view template</name>
  <files>views/agents/attendance/agent-attendance.view.php</files>
  <action>
Create `views/agents/attendance/agent-attendance.view.php`. This view receives `$classes` (array of class rows) and `$agentId` (int).

Layout (use Phoenix Bootstrap classes per CLAUDE.md — check Phoenix extracted folder for patterns):

1. Page header: `h4` with "My Classes" title

2. If `$classes` is empty: show alert-info "No classes assigned to you."

3. If classes exist: render a responsive card list (one card per class):
   - Each card shows: class_code (bold), class_subject, class_type badge, class_status badge
   - Status badges: use `badge badge-phoenix badge-phoenix-success` for active, `badge-phoenix-warning` for stopped, `badge-phoenix-secondary` for other
   - Each card has a "View / Capture Attendance" link button (`btn btn-phoenix-primary btn-sm`) pointing to `/app/display-single-class/?class_id={class_id}`
   - Use Bootstrap row/col grid: `col-md-6 col-lg-4` per card for responsive layout

4. Use `htmlspecialchars()` on all output values (XSS prevention per codebase convention).

5. Do NOT add any custom CSS — use only existing Phoenix/Bootstrap classes. Check `/home/laudes/zoot/projects/phoenix/phoenix-extracted` for class names if needed.
  </action>
  <verify>
    <automated>php -l views/agents/attendance/agent-attendance.view.php</automated>
  </verify>
  <done>View template renders agent's class list with Phoenix styling, links to single-class page for attendance capture. No syntax errors.</done>
</task>

</tasks>

<verification>
1. `php -l src/Agents/Controllers/AgentAccessController.php` — no syntax errors
2. `php -l views/agents/attendance/agent-attendance.view.php` — no syntax errors
3. Controller has all 5 methods: registerShortcodes, ensureAttendancePage, agentAttendanceShortcode, resolveAgentId, getClassesForAgent
4. View handles empty classes array gracefully
5. JSONB query uses integer cast for agent_id
</verification>

<success_criteria>
- AgentAccessController registers `[wecoza_agent_attendance]` shortcode
- Shortcode resolves WP user -> agent_id -> assigned classes via JSONB containment query
- View renders class cards with links to existing single-class page
- Page auto-creation creates `/agent-attendance/` if missing
- Orphaned WP agent (no agent record) sees friendly error, not crash
</success_criteria>

<output>
After completion, create `.planning/phases/55-agent-attendance-page/55-01-SUMMARY.md`
</output>
