---
phase: 61-all-absent-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/js/classes/attendance-capture.js
autonomous: false
requirements:
  - ATT-01
  - ATT-02

must_haves:
  truths:
    - "When all learners have 0 hours present, a confirmation dialog appears before submission"
    - "Cancelling the dialog re-enables the submit button and prevents AJAX call"
    - "Confirming the dialog proceeds with normal submission (session saved)"
    - "Dialog does NOT appear when at least one learner has hours > 0"
  artifacts:
    - path: "assets/js/classes/attendance-capture.js"
      provides: "All-absent detection guard in submitCapture()"
      contains: "allAbsent"
  key_links:
    - from: "submitCapture() validation pass"
      to: "$.ajax() call"
      via: "window.confirm() guard"
      pattern: "window\\.confirm.*all.*absent"
---

<objective>
Add an all-absent confirmation guard to the attendance capture submit flow.

Purpose: Prevent accidental submission of sessions where every learner has 0 hours present — agents must explicitly confirm this is intentional.
Output: Modified `submitCapture()` function in attendance-capture.js with window.confirm() guard.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-all-absent-confirmation/61-RESEARCH.md

@assets/js/classes/attendance-capture.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all-absent detection and confirmation guard to submitCapture()</name>
  <files>assets/js/classes/attendance-capture.js</files>
  <action>
In `submitCapture()`, insert the following block AFTER the `if (!isValid)` early-return (after line 830) and BEFORE the `$.ajax({` call (line 832):

```javascript
// [ATT-01] Detect all-absent session
const allAbsent = learnerHours.length > 0 &&
    learnerHours.every(function(l) { return l.hours_present === 0; });

// [ATT-02] Prompt agent to confirm before submission
if (allAbsent) {
    if (!window.confirm(
        'All learners have 0 hours present for this session.\n\n' +
        'Do you want to record this as an all-absent session?'
    )) {
        $btn.prop('disabled', false).html(
            '<i class="bi bi-check-lg me-1"></i>' + btnLabel
        );
        return;
    }
}
```

Key requirements:
- Guard MUST come AFTER validation (after the `isValid` check) so invalid inputs (NaN) are caught first
- Guard MUST come BEFORE `$.ajax()` so the AJAX call is prevented on cancel
- `learnerHours.length > 0` prevents vacuous truth on empty array
- Use `window.confirm()` — matches existing pattern at line 1054 (`adminDeleteSession`)
- On cancel: re-enable button with same icon+label pattern used at lines 826-828
- On confirm: fall through to existing `$.ajax()` — no changes to AJAX call needed
- Do NOT modify any other function or add any new files
  </action>
  <verify>
    <automated>grep -n "allAbsent" assets/js/classes/attendance-capture.js | head -5</automated>
  </verify>
  <done>
- `allAbsent` detection variable exists after validation block
- `window.confirm()` call exists with all-absent message
- Cancel path re-enables button and returns early
- Confirm path falls through to existing $.ajax() call
- No other functions or files modified
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all-absent confirmation guard in browser</name>
  <files>assets/js/classes/attendance-capture.js</files>
  <action>
Human verifies the all-absent confirmation guard works correctly in the browser.

What was built: All-absent confirmation guard in the attendance capture submit flow.

Steps to verify:
1. Open a single class page with enrolled learners
2. Open the attendance capture modal for any date
3. Set ALL learners' hours present to 0 (keep page numbers valid)
4. Click Submit Attendance
5. VERIFY: A browser confirmation dialog appears asking about all-absent session
6. Click Cancel — VERIFY: Button re-enables, no network request fires (check DevTools Network tab)
7. Click Submit again, dialog appears again
8. Click OK — VERIFY: Session saves normally, toast success message appears
9. Now set ONE learner's hours to 1, keep others at 0
10. Click Submit — VERIFY: No dialog appears, submission proceeds directly
  </action>
  <verify>User confirms "approved" or describes issues</verify>
  <done>All four scenarios pass: dialog appears on all-zero, cancel re-enables button, confirm saves, mixed hours skip dialog</done>
</task>

</tasks>

<verification>
- `grep -c "allAbsent" assets/js/classes/attendance-capture.js` returns at least 2 (declaration + usage)
- `grep -c "window.confirm" assets/js/classes/attendance-capture.js` returns at least 2 (existing + new)
- The new code block sits between the `if (!isValid)` return and the `$.ajax({` call
</verification>

<success_criteria>
- ATT-01: All-zero hours_present detection works for any number of learners (1+)
- ATT-02: Agent sees confirm dialog and can cancel (re-enables button) or proceed (saves normally)
- No regression: mixed-hours submissions (at least one learner with hours > 0) proceed without dialog
</success_criteria>

<output>
After completion, create `.planning/phases/61-all-absent-confirmation/61-01-SUMMARY.md`
</output>
